

// 2. Write a program to have 3 variables (all three are number) and output the highest/greatest/maximum of all three.

function find(){ 

    var a = 40;
    var b = 50;
    var c = 300;

        if(a > b && a > c){

            console.log('A is Greater i.e. =', a);
        }
        else if(b > c){

            console.log('B is Greater i.e. =', b);
        }
         else{
        console.log('C is Greater i.e. =', c);
         }
}
